import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        marginTop: 25,
        padding: 8,
        // alignItems: 'center',
        // justifyContent: 'center',
    },

    inputView: {
        borderRadius: 30,
        width: "70%",
        height: 45,
        padding: 10,
    },

});